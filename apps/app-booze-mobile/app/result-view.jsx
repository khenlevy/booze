/**
 * Legacy route — drink detail now uses /search-results with the same params.
 */
import SearchResultsScreen from './search-results';

export default function ResultViewScreen() {
  return <SearchResultsScreen />;
}
