# @booze/cl-form-rn

React Native form primitives without Expo: `RatingSelector`, `QuantityInput`, `DateTimePicker`, optional `FormThemeProvider` / `useFormTheme`. Icons are injectable (`StarComponent`, `icons.*`).

Mobile apps using Expo should prefer **`@booze/mb-form-expo`**, which wires Material icons and the Booze theme.
